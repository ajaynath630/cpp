#include <iostream>
#define vector <vector<int>> vect

using namespace std;

 get_d(vector<int> &c) {
  
  vector<int> :: iterator it;
  for(it=c.begin();it!=c.end():c++) {
      cin>>(*c);
  }
  
  ret
}

int main() {

vect a;

vector <int> a;

get_d(a);


} 